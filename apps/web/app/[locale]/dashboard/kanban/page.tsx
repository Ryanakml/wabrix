import { DashboardPlaceholder } from "@/components/dashboard/dashboard-placeholder";

export default function KanbanPage() {
  return (
    <DashboardPlaceholder
      eyebrow="Template Shell"
      title="Kanban placeholder"
      description="This route holds the exact imported shell navigation target for the template layout. Functional kanban wiring can be layered in later without reshaping the sidebar."
    />
  );
}
