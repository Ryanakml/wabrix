import { DashboardPlaceholder } from "@/components/dashboard/dashboard-placeholder";

export default function ProductPage() {
  return (
    <DashboardPlaceholder
      eyebrow="Template Shell"
      title="Product placeholder"
      description="This placeholder preserves the template product navigation slot. Real product content can replace this page later without changing the shell."
    />
  );
}
