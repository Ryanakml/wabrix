import { DashboardPlaceholder } from "@/components/dashboard/dashboard-placeholder";

export default function UsersPage() {
  return (
    <DashboardPlaceholder
      eyebrow="Template Shell"
      title="Users placeholder"
      description="This placeholder keeps the template navigation structure intact while the real Wabrix users experience is wired in separately."
    />
  );
}
