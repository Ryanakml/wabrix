import { DashboardPlaceholder } from "@/components/dashboard/dashboard-placeholder";

export default function WorkspacesPage() {
  return (
    <DashboardPlaceholder
      eyebrow="Template Shell"
      title="Workspaces placeholder"
      description="This placeholder route exists so the imported template sidebar and organization switcher have a stable target before deeper workspace wiring is added."
    />
  );
}
